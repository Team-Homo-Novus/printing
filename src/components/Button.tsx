export default function Button({children}:any) {
    return <button type="button" className="relative w-full px-6 py-2 text-white overflow-hidden font-semibold rounded bg-indigo-600">{children}</button>
}